/**
 * @param {string} s
 * @return {string}
 */
var clearDigits = function(s) {
    let stack = [];

    for(let i = 0; i<s.length; i++){
        if(s[i] <=9) {
            stack.pop();
            continue;
        }
        stack.push(s[i]);
    }

    return stack.join("")
};
